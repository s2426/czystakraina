<?php
/*
$current -> illustrations_potential = get_user_illustrations($user);

foreach($current -> illustrations_potential as &$image)
{
  $image = illustration_file_to_pretty($image);
}

$current -> illustrations_potential = implode("\n", $current -> illustrations_potential);
*/
?>
